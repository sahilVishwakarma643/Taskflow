spring:
  application:
    name: taskflow
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: rootsahil@21
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
  server:
    port: 8080
